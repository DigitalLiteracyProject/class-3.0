import { Component } from 'react';

export default class ClassAdmin extends Component {
    render() {
        return (<div>ClassAdmin not implemented. Current classroom {this.props.match.params.classroomId}</div>);
    }
}
